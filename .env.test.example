# Census Terraform Provider - Test Environment Configuration
# Copy this file to .env.test and fill in your credentials
# NOTE: .env.test is gitignored and should NEVER be committed

# Required for integration tests
TF_ACC=1

# Census Staging API
CENSUS_BASE_URL='https://app.staging.getcensus.com/api/v1'
CENSUS_PERSONAL_ACCESS_TOKEN='your-staging-personal-access-token'

# Redshift Test Database
# Use a test/dev cluster, not production
CENSUS_TEST_REDSHIFT_HOST='your-cluster.region.redshift.amazonaws.com'
CENSUS_TEST_REDSHIFT_PORT='5439'
CENSUS_TEST_REDSHIFT_DATABASE='dev'
CENSUS_TEST_REDSHIFT_USERNAME='testuser'
CENSUS_TEST_REDSHIFT_PASSWORD='your-password'

# Salesforce Sandbox (JWT OAuth)
# Use a sandbox, not production
CENSUS_TEST_SALESFORCE_USERNAME='test@example.com'
CENSUS_TEST_SALESFORCE_INSTANCE_URL='https://your-sandbox.develop.my.salesforce.com'
CENSUS_TEST_SALESFORCE_CLIENT_ID='3MVG9...'
CENSUS_TEST_SALESFORCE_JWT_SIGNING_KEY='-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n'
CENSUS_TEST_SALESFORCE_DOMAIN='test.salesforce.com'

# Setup Instructions:
# 1. Get Census staging credentials from https://app.staging.getcensus.com/settings/developer
# 2. Set up a test Redshift cluster with read-only access
# 3. Configure Salesforce sandbox with JWT OAuth (see TESTING.md)
# 4. Copy this file: cp .env.test.example .env.test
# 5. Fill in your credentials
# 6. Run tests: make test-integration
#
# See TESTING.md for detailed setup instructions
